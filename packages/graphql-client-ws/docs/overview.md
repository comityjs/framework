# @comity/graphql-client-ws — Overview

`@comity/graphql-client-ws` adapts GraphQL WebSocket clients to the `@comity/graphql-client` transport contract.

It exists as a concrete transport option for GraphQL operations that are executed over WebSocket.
