# @comity/graphql-builder — Overview

`@comity/graphql-builder` defines the structural pieces used to build GraphQL query documents.

It is a contract-and-serialization package, not a transport package.
