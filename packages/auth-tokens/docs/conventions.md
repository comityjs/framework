# @comity/auth-tokens — Conventions

`@comity/auth-tokens` is a support package.

## Rules

- keep the API small and contract-oriented
- depend on `@comity/auth` and `@comity/primitives` only
- do not introduce framework or adapter types

