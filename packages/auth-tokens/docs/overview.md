# @comity/auth-tokens — Overview

`@comity/auth-tokens` hosts token-related contracts shared by Comity auth
packages.

## What it includes

- token envelope contracts
- token facade contracts
- a default facade implementation

## What it does not include

- auth session orchestration
- HTTP transport integration
- storage adapters

