# @comity/media — Overview

`@comity/media` is the minimal contract package for media models in Comity.

It exists so higher-level packages can refer to a shared media shape without pulling in storage or rendering concerns.
