# @comity/router-path-to-regexp — Overview

`@comity/router-path-to-regexp` adapts the `path-to-regexp` matcher to the Comity router contracts.

It exists as a concrete router implementation that can be swapped without affecting the routing abstraction layer.
